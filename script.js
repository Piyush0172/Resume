// This script file is for adding interactivity to the portfolio website

// Add event listeners when the DOM content is loaded
document.addEventListener('DOMContentLoaded', function () {
    // Example: Add a click event listener to a button
    const button = document.querySelector('.button-class'); // Replace '.button-class' with the actual class of your button
    if (button) {
        button.addEventListener('click', function () {
            // Perform actions when the button is clicked
            console.log('Button clicked!');
            // You can add more code here to perform specific actions
        });
    }

    // Add more event listeners and interactivity as needed
});
